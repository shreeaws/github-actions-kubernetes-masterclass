name: CI pipeline


jobs:
    build-&-push:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4                # Check out the repository code to the runner
            
            - name: set up Docker Buildx
              uses: docker/setup-buildx-action@v4      # Set up Docker Buildx


            